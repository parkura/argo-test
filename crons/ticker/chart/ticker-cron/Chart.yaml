apiVersion: v2
name: ticker-cron
description: GitOps deployment simulation — ticker-cron
type: application
# version/appVersion are placeholders; CI sets them via
# helm package --version 1.0.<run> --app-version 1.0.<run>-<sha7>
version: 0.0.0
appVersion: "0.0.0"
