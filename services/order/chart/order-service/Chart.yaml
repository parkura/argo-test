apiVersion: v2
name: order-service
description: GitOps deployment simulation — order-service
type: application
# version/appVersion are placeholders; CI sets them via
# helm package --version 1.0.<run> --app-version 1.0.<run>-<sha7>
version: 0.0.0
appVersion: "0.0.0"
